package collection_map.map;

/*
 * Map常用方法：
 *       V put(K key, V value)：将指定的key-value添加到(当前)映射中，返回null
 *       V get(Object key)：返回指定key对应的value
 *       V remove(Object key)：删除指定key对应的数据，并返回value
 *       boolean containsKey(Object key)：判断当前映射中是否包含指定的key
 *       boolean containsValue(Object value)：判断当前映射中是否包含指定的value
 *       void clear()：清空映射中的所有数据
 *       int size()：返回映射中数据的个数
 *       boolean isEmpty()：判断映射是否为空
 *       Set<K> keySet()：返回所有key构成的Set集合
 * */

public class _02 {
}
