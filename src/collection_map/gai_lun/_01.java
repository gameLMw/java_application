package collection_map.gai_lun;

/*
 * 集合
 * 内存层面需要针对多个数据进行存储，容器有，数组，集合等
 *
 * 数据存储多个数据的特点
 *   数组一旦初始化，长度是固定的，不可变
 *   数组中多个元素是依次紧密存放的，有序的，可重复的
 *   数组存储数据，只能存储一种数据类型，但数据安全
 *
 * Java集合框架体系（java.util包）
 *   java.util.Collection接口：存储一个一个的数据
 *       List接口：存储有序的，可重复的数据（动态数组）
 *           ArrayList（主要实现类）、LinkedList、Vector
 *       Set接口：存储无序的，不可重复的数据（集合）
 *           HashSet（主要实现类）、LinkedHashSet、TreeSet
 *   java.util.Map接口：存储一对一对的数据（key-value键值对，函数，y=f(x)）
 *       HashMap（主要实现类）、LinkedHashMap、TreeMap、Hashtable、Properties
 * */

public class _01 {
}
